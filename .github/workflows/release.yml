name: Release

# Trigger by pushing a version tag: git tag v1.2.3 && git push origin v1.2.3
on:
  push:
    tags:
      - "v*"

jobs:
  release:
    name: Build & publish signed release APK
    runs-on: ubuntu-latest

    permissions:
      contents: write   # needed to create the GitHub Release

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      # ------------------------------------------------------------------ #
      # Signing — requires four repository secrets:                         #
      #   KEYSTORE_BASE64   base64-encoded .jks file                        #
      #     encode with: base64 -w 0 kraken-release.jks                     #
      #   KEYSTORE_PASSWORD keystore password                               #
      #   KEY_ALIAS         key alias inside the keystore                   #
      #   KEY_PASSWORD      key password                                    #
      # ------------------------------------------------------------------ #
      - name: Decode keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > "$RUNNER_TEMP/release.jks"

      - name: Build signed release APK
        run: ./gradlew assembleRelease --no-daemon
        env:
          KEYSTORE_PATH: ${{ runner.temp }}/release.jks
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}

      - name: Upload release APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: kraken-bridge-${{ github.ref_name }}
          path: app/build/outputs/apk/release/*.apk
          retention-days: 30

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: app/build/outputs/apk/release/*.apk
          generate_release_notes: true
          fail_on_unmatched_files: true
