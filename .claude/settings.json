{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(adb install -r *)",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Project sideload rule (memory: project_sideload_workflow.md): adb install -r preserves app state and hides fresh-install bugs. Use the clean sequence: adb shell am force-stop ch.fbc.krakenbridge && adb uninstall ch.fbc.krakenbridge && adb install app/build/outputs/apk/debug/app-debug.apk\"}}'"
          }
        ]
      }
    ]
  }
}
